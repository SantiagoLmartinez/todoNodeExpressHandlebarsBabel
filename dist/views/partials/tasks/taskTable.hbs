{{#if tasks}}
<table class="table table-dark  table-striped">
    <thead>
        <tr>
            <th>N°</th>
            <th>title</th>
            <th>description</th>
            <th>Operations</th>
        </tr>
    </thead>
    <tbody>
        {{#each tasks }}
        <tr>
            <td>{{@index}}</td>
            <td>{{title}}</td>
            <td>{{description}}</td>
            <td>
                {{#if done}}
                <a href="/tasks/{{_id}}/toggleDone" class="btn btn-primary mx-1">UnDone</a>
                {{else}}
                <a href="/tasks/{{_id}}/toggleDone" class="btn btn-secondary mx-1">Done</a>
                {{/if}}

                <a href="/tasks/{{_id}}/edit" class="btn btn-warning mx-1">Edit</a>
                <a href="/tasks/{{_id}}/delete" class="btn btn-danger mx-1">Delete</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<div class="text-center">
<h1>NO TASKS</h1>
</div>
{{/if}}